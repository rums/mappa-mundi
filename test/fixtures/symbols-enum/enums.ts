// Basic enum
export enum Direction {
  Up,
  Down,
  Left,
  Right,
}

// String enum
export enum Color {
  Red = 'red',
  Green = 'green',
  Blue = 'blue',
}

// Const enum
export const enum Flags {
  A,
  B,
  C,
}
